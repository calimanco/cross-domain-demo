extends layout.pug

block head
  title WindowHash Work Page

block content
  p This is a work page for WindowHash, which is generated by server2.

block scripts
  script.
    window.parent.location.replace(`#{refererUrl}!{'#'}!{encodeURIComponent(JSON.stringify(res))}`);
