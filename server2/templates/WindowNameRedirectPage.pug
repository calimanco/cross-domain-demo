extends layout.pug

block head
  title WindowName Redirect Page

block content
  p This is a redirect page for WindowName, which is generated by server2.

block scripts
  script.
    window.name = `!{JSON.stringify(res)}`;
    setTimeout(() => {
      window.location.replace("#{redirect}");
    });
